<div class="top-row pl-4 navbar navbar-dark">
    <a class="navbar-brand" href="">Talper Examen</a>
    <button class="navbar-toggler" @onclick="ToggleNavMenu">
        <span class="navbar-toggler-icon"></span>
    </button>
</div>
<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <ul class="nav flex-column">



        <AuthorizeView Roles="Admin">
            <li class="nav-item px-3">
                <NavLink class="nav-link" href="ProspectoView">
                    <span class="oi oi-file" aria-hidden="true"></span>Cliente
                </NavLink>
            </li>
        </AuthorizeView>
        <AuthorizeView Roles="Admin">
            <li class="nav-item px-3">
                <NavLink class="nav-link" href="DocumentoView">
                    <span class="oi oi-file" aria-hidden="true"></span>Documento
                </NavLink>
            </li>
        </AuthorizeView>
        <li class="nav-item px-3">
            <NavLink class="nav-link" href="CuentaView">
                <span class="oi oi-list-rich" aria-hidden="true"></span>Cuenta
            </NavLink>
        </li>
        <AuthorizeView Roles="Admin">
            <li class="nav-item px-3">
                <NavLink class="nav-link" href="AntiguedadSaldo">
                    <span class="oi oi-file" aria-hidden="true"></span>AntiguedadSaldo
                </NavLink>
            </li>
        </AuthorizeView>



    </ul>
</div>


@code {
    private bool collapseNavMenu = true;
    private bool expandSubNav;

    private string NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
        if (!expandSubNavSettings)
        {
            collapseNavMenu = !collapseNavMenu;
        }
        if (!expandSubNavSettingsCatalogo)
        {
            collapseNavMenu = !collapseNavMenu;
        }
    }



    private bool expandSubNavSettingsCatalogo;
    private bool expandSubNavSettings;


    string baseMenuClass = "navbar-collapse d-sm-inline-flex flex-sm-row-reverse";



}
